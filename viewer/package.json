{
  "name": "grovewatch-viewer",
  "version": "0.1.0",
  "description": "Zero-dependency browser viewer for grovewatch provenance snapshots.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test",
    "serve": "node ../scripts/serve.mjs"
  },
